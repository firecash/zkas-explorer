# Production build for explorer.firecash.info.
# The SPA is served same-origin; nginx reverse-proxies /api → firecash-api (:8500).
VITE_API_BASE=/api
VITE_SHIELDED_API_BASE=/api
VITE_SOCKET_URL=wss://explorer.firecash.info
